using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;

namespace CloudCash.Test.Client.Factory
{
    [TestClass]
    public class MenuItemFactoryTests
    {
        [TestMethod]
        public void AllEnumValuesAreDeclared()
        {
            //var enumValues = Enum.GetValues(MenuItemType.Get);

            //foreach (var menuItemType in enumValues)
            //{

            //}
        }
    }
}
